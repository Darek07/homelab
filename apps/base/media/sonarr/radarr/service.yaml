apiVersion: v1
kind: Service
metadata:
  labels:
    app: sonarr
  name: sonarr
  namespace: media
spec:
  ports:
    - name: web
      port: 80
      protocol: TCP
      targetPort: 8989
  selector:
    app: sonarr
  type: ClusterIP
