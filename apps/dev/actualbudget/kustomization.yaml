apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/actualbudget
  - hostname.yaml
  - tunnel-token.yaml

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: actualbudget
